import types from '../ActionType'
export  const WEATHER_ACTION = {
   type:types.WEATHER_TYPE,
   payload:{
       weather:null
   }
} 
export  const FORECAST_ACTION = {
   type:types.FORECAST_TYPE,
   payload:{
       forecast:null
   }
} 
